analyzer:
  allow_dynamic_versions: true
  skip_excluded: true

excludes:
  paths:
    - pattern: 'apps/**'
      reason: 'BUILD_TOOL_OF'
      comment: 'Ignore dev app; not intended for distribution'
    - pattern: 'docs/.vite/deps/**'
      reason: 'BUILD_TOOL_OF'
      comment: 'Ignore Vite-generated dependency folder'
    - pattern: '**/.vite/**'
      reason: 'BUILD_CACHE'
      comment: 'Ignore all Vite cache'

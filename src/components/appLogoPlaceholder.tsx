import React from "react";
import { APPLICATION_LOGO_PLACEHOLDER } from "../constants";

export const AppLogoPlaceholder = () => {
  return <span style={{marginLeft: "20px"}}>{APPLICATION_LOGO_PLACEHOLDER}</span>;
};
